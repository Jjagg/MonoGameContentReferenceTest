To test this, copy the .targets file over the original file in your
MonoGame installation folder, then just build the project.

I find the easiest way to test to first run msbuild/xbuild from the 
a terminal/cmd, then run the 'tree' command on the bin folder 
or just check it using a file manager.